% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/obs_groups.R
\name{ensure_config_obs_groups}
\alias{ensure_config_obs_groups}
\title{Build \code{config$obs_groups} from design / ELGM when missing}
\usage{
ensure_config_obs_groups(
  config,
  A,
  elgm_matrix = NULL,
  num_shards = NULL,
  num_threads = 1L
)
}
\arguments{
\item{config}{Config list (may already contain \code{obs_groups}).}

\item{A}{Random-effects design (\code{term_data$A}).}

\item{elgm_matrix}{Optional ELGM stratum map.}

\item{num_shards}{Maximum shards (from \code{config$num_shards} if missing).}

\item{num_threads}{Used for \code{min_shards} when ELGM is present.}
}
\value{
\code{config} with \code{obs_groups} filled when possible.
}
\description{
Used by \code{\link{adlaplace}} and \code{\link{ad_pack}} so that
\code{num_shards} alone is enough for case-crossover (ELGM) models.
When \code{elgm_matrix} has columns, shards index strata, not raw rows of
\code{y}.
}
\keyword{internal}
