% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deriv_scan.R
\name{deriv_scan_joint}
\alias{deriv_scan_joint}
\title{Scan joint log-density AD derivatives vs finite differences}
\usage{
deriv_scan_joint(
  ad_pack,
  x,
  index,
  width = 0.1,
  n = 11L,
  ad_shards = NULL,
  negative = FALSE
)
}
\arguments{
\item{ad_pack}{An \code{ad_pack} object (typically with \code{num_threads = 1}).}

\item{x}{Numeric full parameter vector.}

\item{index}{Integer (1-based) coordinate of \code{x} to vary.}

\item{width}{Half-width of the scan interval around \code{x[index]}.}

\item{n}{Integer number of grid points (default 11).}

\item{ad_shards}{Optional 0-based shard indices passed to
\code{\link{joint_log_dens}} / \code{\link{grad}} / \code{\link{hessian}}.}

\item{negative}{Logical; passed through to the joint-density evaluators.}
}
\value{
A list with:
  \item{grid}{Coordinate values (length \code{n}).}
  \item{mid}{Midpoints for finite differences (length \code{n - 1}).}
  \item{value}{Joint density along the grid.}
  \item{fd_value}{Midpoint differences of \code{value}.}
  \item{grad}{\code{n x length(x)} matrix of AD gradients (rows = grid).}
  \item{fd_grad}{\code{(n-1) x length(x)} midpoint slopes of \code{grad}.}
  \item{hess_row}{\code{n x length(x)} AD Hessian rows for \code{index}.}
}
\description{
Evaluates the joint log density and its AD gradient/Hessian while varying
one coordinate of the full parameter vector (beta, gamma, theta). Returns
plain matrices shaped for \code{matplot()}/\code{matlines()}.
}
