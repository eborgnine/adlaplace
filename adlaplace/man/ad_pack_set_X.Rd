% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ad_pack.R
\name{ad_pack_set_X}
\alias{ad_pack_set_X}
\title{Rebuild an \code{ad_pack} for a new model-data design}
\usage{
ad_pack_set_X(
  ad_pack,
  model_data,
  config = list(),
  num_threads = 1L,
  reorder_shards = c("none", "gradient", "hessian", "third")
)
}
\arguments{
\item{ad_pack}{Existing \code{ad_pack} whose templates may be reused.}

\item{model_data}{A \code{model_data()} bundle with the same random-effect
structure and (typically) a different fixed-effect design matrix.}

\item{config}{Configuration list passed to \code{\link{ad_pack}}.}

\item{num_threads, reorder_shards}{Forwarded to \code{\link{ad_pack}}.}
}
\value{
A new \code{ad_pack}.
}
\description{
Covariate values in observation \code{XTp} are baked into CppAD tapes at
build time, so they cannot be mutated in place. This helper retapes a new
\code{model_data} bundle and reuses Hessian templates from \code{ad_pack}
whenever sizes and shard counts match (identical sparsity across exposure
samples). Prefer building a list of packs once for modular inference rather
than calling this inside every likelihood evaluation.
}
\seealso{
\code{\link{ad_pack}}, \code{\link{replace_vars}}
}
