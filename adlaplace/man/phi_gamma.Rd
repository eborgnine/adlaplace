% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/amip.R
\name{phi_gamma}
\alias{phi_gamma}
\title{Linear functional of the inner field \eqn{\gamma}}
\usage{
phi_gamma(fit, term = NULL, new_x = NULL, coef = NULL)
}
\arguments{
\item{fit}{An \code{"adlaplace_fit"} from \code{\link{adlaplace}}.}

\item{term}{Optional character name of a covariate whose **random** terms
contribute via \code{\link{design}} (e.g. \code{"pm10"}).}

\item{new_x}{Data frame of prediction points for \code{term} (required when
\code{term} is set). Typically one row, e.g. \code{data.frame(pm10 = 80)}.}

\item{coef}{Optional gamma label (or labels) to set to 1 (unit / contrast
vectors), e.g. a treatment coefficient name.}
}
\value{
Named numeric vector of length \code{n_gamma}.
}
\description{
Builds a named numeric vector \eqn{a} aligned to
\code{fit$model_data$term_data$info$gamma$gamma_label} so that
\eqn{\phi(\gamma) = a^\top\gamma}.
}
\seealso{
\code{\link{influence_scores}}, \code{\link{amip_set}},
  \code{\link{design}}, \code{\link{sim_random}}
}
