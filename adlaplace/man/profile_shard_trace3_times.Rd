% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{profile_shard_trace3_times}
\alias{profile_shard_trace3_times}
\title{Profile per-shard Reverse3 cost with dummy LinvPt directions}
\usage{
profile_shard_trace3_times(
  handle,
  x,
  half_H_inv,
  trace_columns,
  n_rep = 4L,
  n_warmup = 1L
)
}
\arguments{
\item{handle}{External pointer of class \code{ad_pack_ptr}.}

\item{x}{Parameter vector of length \code{Nparams}.}

\item{half_H_inv}{Sparse Matrix (\code{dgCMatrix}) symbolic pattern.}

\item{trace_columns}{Sparse Matrix (\code{ngCMatrix}/\code{dgCMatrix})
shard-to-column map (\code{dims = c(n_gamma, n_shards)}).}

\item{n_rep}{Number of timed repetitions per shard (mean stored).}

\item{n_warmup}{Number of untimed warmup sweeps per shard.}
}
\value{
Numeric vector of length \code{n_shards} (mean elapsed seconds).
}
\description{
Serial timing helper for \code{ad_pack(..., reorder_shards = "third")}.
Uses the symbolic \code{half_H_inv} CSC pattern (values forced to 1) and
\code{trace_columns} as \code{LinvPtColumns}. Does not require owner
threads or a numeric Cholesky.
}
\keyword{internal}
