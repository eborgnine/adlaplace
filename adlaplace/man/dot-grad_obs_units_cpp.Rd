% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{.grad_obs_units_cpp}
\alias{.grad_obs_units_cpp}
\title{Stream per-unit observation gradients (grad-only tapes)}
\usage{
.grad_obs_units_cpp(model, x, config, units, batch_size, inner, negative)
}
\arguments{
\item{model}{Observation \code{density_data} S4 object.}

\item{x}{Full parameter vector \code{c(beta, gamma, theta)}.}

\item{config}{Config list (merged with grad-only defaults upstream).}

\item{units}{Optional 0-based unit indices; empty means all units.}

\item{batch_size}{Positive integer batch size for temporary tapes.}

\item{inner}{If \code{TRUE}, return inner-\eqn{\gamma} gradients only
(rows are gamma). If \code{FALSE}, rows are the full parameter vector.}

\item{negative}{If \code{TRUE}, negate the density gradient (minimization
sign, matching \code{\link{grad}}).}
}
\value{
A \code{dgCMatrix} with one column per unit.
}
\description{
For each observation unit (ELGM stratum column, or row of \code{y} when
there is no ELGM map), records a temporary one-unit observation tape with
\code{hessian_sparsity = FALSE}, evaluates the sparse gradient at
\code{x}, then destroys the tape. Does not run \code{inner_opt}.
}
\keyword{internal}
