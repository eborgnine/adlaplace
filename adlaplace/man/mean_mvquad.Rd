% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mvn_utils.R
\name{mean_mvquad}
\alias{mean_mvquad}
\title{Posterior mean of random effects via multivariate quadrature}
\usage{
mean_mvquad(
  parameters,
  mode,
  cov,
  ad_fun,
  n = 3L,
  type = "nHN",
  ndConstruction = "sparse",
  dec.type = 2L
)
}
\arguments{
\item{parameters}{Outer \eqn{(\beta, \theta)} at the Laplace fit.}

\item{mode}{Inner mode \eqn{\hat\gamma} (e.g. \code{laplace$opt$solution}).}

\item{cov}{\eqn{H^{-1}} for the inner block (e.g. \code{laplace$extra$hessian$H_inv}).}

\item{ad_fun}{\code{ad_fun} object for \code{joint_log_dens}.}

\item{n}{Quadrature level passed to \pkg{mvQuad} \code{createNIGrid}.}

\item{type}{mvQuad rule (default \code{"nHN"}).}

\item{ndConstruction}{mvQuad grid construction (default \code{"sparse"}).}

\item{dec.type}{Cholesky decomposition flag for \code{mvQuad::rescale}.}
}
\value{
Numeric vector of length \code{n_gamma}.
}
\description{
Approximates \eqn{E[\gamma \mid y]} by importance-weighted quadrature over
\eqn{\gamma}, using a Gaussian proposal \eqn{N(\hat\gamma, H^{-1})} (Laplace
covariance) and correcting with \code{\link{joint_log_dens}}.
}
\details{
Builds a sparse \code{nHN} grid with \pkg{mvQuad}, rescales to the Laplace
proposal, then forms
\eqn{\sum_i w_i \gamma_i \exp(\ell(\gamma_i) - \log q(\gamma_i)) /
\sum_i w_i \exp(\ell(\gamma_i) - \log q(\gamma_i))}
with log-sum-exp stabilization.
}
\seealso{
\code{\link{log_lik_laplace}}, \code{\link{inner_opt}},
  \code{\link{joint_log_dens}}, \code{\link{rmvnldl}}
}
