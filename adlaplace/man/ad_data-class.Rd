% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes.R
\docType{class}
\name{ad_data-class}
\alias{ad_data-class}
\title{Per-shard layout for AD density evaluation}
\description{
Per-shard layout for AD density evaluation
}
\section{Slots}{

\describe{
\item{\code{y}}{Response vector.}

\item{\code{ATp}}{Transpose of random-effects design matrix.}

\item{\code{XTp}}{Transpose of fixed-effects design matrix.}

\item{\code{beta_map}}{Beta parameter map (\code{ngCMatrix}). \code{nrow} is the
global beta block size; column \code{j} has one structural nonzero at row
\code{r} meaning local design column \code{j} of \code{X} uses global beta
\code{r}. When \code{ncol(X) > 0}, require \code{ncol(beta_map) == ncol(X)}.}

\item{\code{gamma_map}}{Gamma parameter map (\code{ngCMatrix}); at most one structural
nonzero per row and column. Same column convention as \code{beta_map} for
local \code{A} columns into the global gamma block.}

\item{\code{theta_map}}{Theta parameter map (\code{ngCMatrix}). Column \code{j}
selects global theta row \code{r} for this shard's theta component \code{j}.}

\item{\code{elgm_matrix}}{Optional exposure-lag map (\code{ngCMatrix}; empty by default).}

\item{\code{ad_fun}}{Registered AD density name for this shard.}

\item{\code{ad_kind}}{Shard kind (\code{"observations"}, \code{"parameters"}, \code{"random"}).}

\item{\code{package}}{Package name whose shared library records tapes for this shard
(defaults to \code{"adlaplace"} when missing).}

\item{\code{precision}}{Optional precision payload (any R object).}

\item{\code{weights}}{Optional per-observation weights (e.g. binomial trial counts).
Empty means all ones.}
}}

