% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{getAdFun}
\alias{getAdFun}
\alias{inner_opt}
\alias{inner_opt_adpack}
\alias{traceHinvT}
\alias{sparsity}
\title{Low-level C++ entry points (Rcpp exports)}
\usage{
getAdFun(data, config, inner = FALSE)

inner_opt(parameters, data, control, config, adPackFull = NULL)

inner_opt_adpack(parameters, adPack, control, config, adPackFull = NULL)

traceHinvT(parameters, LinvPt, LinvPtColumns, config, adPack = NULL)

sparsity(data, config)
}
\arguments{
\item{data}{An R list containing model data and matrices required by the AD
construction. Required fields depend on the model (see vignette).}

\item{config}{An R list of configuration options (e.g., parameter vectors,
sparsity options, threading options).}

\item{inner}{Logical; if TRUE, build the inner AD function; otherwise build
the outer AD function.}

\item{parameters}{Numeric vector of parameters for the requested operation
(e.g., inner optimization or trace calculation). Interpretation depends on
the backend/model.}

\item{control}{Control list for the optimizer (e.g., trust region settings,
tolerances, max iterations). Used by `inner_opt()`.}

\item{adPackFull, adPack}{Optional external pointer (`externalptr`) returned by
`getAdFun()`. If provided, reuse cached AD objects / sparsity structures.}

\item{LinvPt, LinvPtColumns}{Sparse matrix objects (S4, typically from Matrix)
used by `traceHinvT()`; see package documentation/vignette for required
classes and dimensions.}
}
\value{
`getAdFun()` returns an external pointer (`externalptr`) to an
  internal AD object.
}
\description{
Low-level C++ entry points (Rcpp exports)
}
\details{
The returned pointer is not human-readable and should not be modified.
It may hold substantial memory (tapes, sparsity patterns, caches). Use
package-level functions to manage lifecycle and computation.
}
