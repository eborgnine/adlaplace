% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ad_pack.R
\name{adlaplace_cpp}
\alias{adlaplace_cpp}
\alias{joint_log_dens}
\alias{grad}
\alias{hessian}
\alias{trace_hinv_t}
\alias{fun_obj_fdfh}
\title{C++ backend entry points}
\usage{
joint_log_dens(ad_pack, x, ad_shards = NULL, negative = TRUE)

grad(ad_pack, x, ad_shards = NULL, inner = FALSE, negative = TRUE)

hessian(
  ad_pack,
  x,
  ad_shards = NULL,
  inner = FALSE,
  verbose = FALSE,
  negative = TRUE
)

trace_hinv_t(ad_pack, x, LinvPt, LinvPtColumns, verbose = FALSE)

fun_obj_fdfh(ad_pack, parameters, gamma, inner = TRUE, verbose = FALSE)
}
\arguments{
\item{ad_pack}{An \code{ad_pack} S4 object or \code{ad_pack_ptr} handle.}

\item{x}{Numeric parameter vector of length \code{Nparams}.}

\item{ad_shards}{Optional integer vector of 0-based shard indices; \code{NULL} or
\code{integer(0)} evaluates all shards.}

\item{negative}{Logical (default \code{TRUE}). If \code{TRUE}, return the
**negative** log density \eqn{-\ell(x)} and its derivatives (minimization /
\pkg{trustOptim} sign, consistent with \code{inner_opt()}). If \code{FALSE},
return \eqn{\ell(x)}, \eqn{\nabla\ell}, and \eqn{\nabla^2\ell}.}

\item{inner}{Logical; if \code{TRUE}, evaluate inner-\eqn{\gamma}
derivatives; if \code{FALSE}, evaluate outer derivatives at the full
parameter vector.}

\item{verbose}{Logical; if \code{TRUE}, print threads, shards, and CppAD
session phases.}

\item{LinvPt, LinvPtColumns}{Sparse factors for \code{trace_hinv_t()}.}

\item{parameters}{Numeric vector of length \code{Nbeta + Ntheta}.}

\item{gamma}{Numeric vector of length \code{Ngamma}.}
}
\value{
See individual functions.
}
\description{
Low-level entry points exposed to R. Accept an \code{ad_pack} S4 object
(via \code{@ptr}) or a raw \code{ad_pack_ptr}.
}
\section{Sign convention}{

With default \code{negative = TRUE}, \code{joint_log_dens()}, \code{grad()},
and \code{hessian()} match \code{inner_opt()} (negative log-density). Set
\code{negative = FALSE} for the joint log density and its derivatives at the
same \code{x}.
}

\section{Thread affinity}{

These are **serial debug** APIs. They error if the handle was built with
\code{ad_pack(..., num_threads > 1)}. Use a plain \code{ad_pack_ptr} (or
\code{ad_pack(..., num_threads = 1)}), or \code{\link{clone_ad_pack_ptr}()}
before assigning multi-thread affinity. Parallel evaluation uses
\code{\link{fun_obj_fdfh}} / \code{\link{inner_opt}} /
\code{\link{log_lik_laplace}} on the affined handle.
}

