% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R, R/z_ad_fun_s4_wrappers.R
\name{adlaplace_cpp}
\alias{adlaplace_cpp}
\alias{joint_log_dens}
\alias{grad}
\alias{hessian}
\alias{traceHinvT}
\title{C++ backend entry points}
\usage{
joint_log_dens(ad_fun_ptr, x, shards = NULL, negative = TRUE)

grad(ad_fun_ptr, x, shards = NULL, inner = FALSE, negative = TRUE)

hessian(
  ad_fun_ptr,
  x,
  shards = NULL,
  inner = FALSE,
  verbose = FALSE,
  negative = TRUE
)

traceHinvT(ad_fun_ptr, x, LinvPt, LinvPtColumns, num_threads, shards = NULL)

joint_log_dens(ad_fun_ptr, x, shards = NULL, negative = TRUE)

grad(ad_fun_ptr, x, shards = NULL, inner = FALSE, negative = TRUE)

hessian(
  ad_fun_ptr,
  x,
  shards = NULL,
  inner = FALSE,
  verbose = FALSE,
  negative = TRUE
)

traceHinvT(ad_fun_ptr, x, LinvPt, LinvPtColumns, num_threads, shards = NULL)
}
\arguments{
\item{ad_fun_ptr}{External pointer of class \code{ad_fun_ptr}.}

\item{x}{Numeric parameter vector of length \code{Nparams}.}

\item{shards}{Optional integer vector of 0-based shard indices; \code{NULL} or
\code{integer(0)} evaluates all shards.}

\item{negative}{Logical (default \code{TRUE}). If \code{TRUE}, return the
**negative** log density \eqn{-\ell(x)} and its derivatives (minimization /
\pkg{trustOptim} sign, consistent with \code{inner_opt()}). If \code{FALSE}, return \eqn{\ell(x)}, \eqn{\nabla\ell},
and \eqn{\nabla^2\ell}.}

\item{inner}{Logical scalar for inner-\eqn{\gamma} vs outer derivatives.}

\item{verbose}{Logical passed to \code{hessian()}.}

\item{LinvPt, LinvPtColumns, num_threads}{See \code{traceHinvT()}.}
}
\value{
Scalar log-density value (sign per \code{negative}).

Gradient of log density (sign per \code{negative}).

Sparse Hessian of log density (sign per \code{negative}).
}
\description{
Low-level C++ entry points exposed to R via Rcpp.
}
\section{Functions}{
\itemize{
\item \code{joint_log_dens()}: Accept \code{ad_fun} S4 (uses \code{@ptr}).

\item \code{grad()}: Accept \code{ad_fun} S4 (uses \code{@ptr}).

\item \code{hessian()}: Accept \code{ad_fun} S4 (uses \code{@ptr}).

\item \code{traceHinvT()}: Accept \code{ad_fun} S4 (uses \code{@ptr}).

}}
\section{Sign convention}{

With default \code{negative = TRUE}, \code{joint_log_dens()}, \code{grad()},
and \code{hessian()} match \code{inner_opt()} (negative
log-density). Set \code{negative = FALSE} for the joint log density and its
derivatives at the same \code{x}.
}

