% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/families.R
\docType{class}
\name{gaussian-class}
\alias{gaussian-class}
\alias{gaussian}
\alias{theta_info,gaussian-method}
\title{Gaussian observation term}
\usage{
gaussian(
  x,
  init = 1,
  lower = .my_theta_lower,
  upper = .my_theta_upper,
  parscale = .my_theta_parscale
)

\S4method{theta_info}{gaussian}(term)
}
\arguments{
\item{x}{Outcome variable name.}

\item{init}{Initial value for the residual standard deviation.}

\item{lower}{Lower bound for the residual standard deviation.}

\item{upper}{Upper bound for the residual standard deviation.}

\item{parscale}{Parameter scale for optimization.}

\item{term}{A model term object (S4 methods).}

\item{data}{A data frame containing the variables (S4 methods).}
}
\value{
A \code{gaussian} object (or a \code{stats::family} when called
  with no arguments).
}
\description{
Model term for the observation-level Gaussian log density
registered as \code{gaussian_obs}. This is the default observation term:
a bare response symbol on the left-hand side of a formula (e.g.
\code{y ~ x}) is coerced to \code{gaussian(y)} by
\code{\link{collect_terms}}.

Creates a response term wired to the \code{gaussian_obs} AD
shard, with a single residual standard deviation parameter (log scale
during optimization).

When called with no arguments this function falls back to
\code{stats::gaussian()}, so \code{glm(..., family = gaussian)} keeps
working with \pkg{adlaplace} attached.
}
\details{
Gaussian observation model term
}
\section{Functions}{
\itemize{
\item \code{theta_info(gaussian)}: Theta info for the residual standard deviation.

}}
\section{Slots (inherited from \code{model})}{

\describe{
  \item{\code{ad_fun}}{Character scalar \code{"gaussian_obs"}.}
  \item{\code{ad_kind}}{Character scalar \code{"observations"}.}
}
}

