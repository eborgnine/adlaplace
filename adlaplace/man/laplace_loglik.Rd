% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/log_lik.R
\name{laplace_loglik}
\alias{laplace_loglik}
\title{Laplace-approximate log-likelihood (positive convention)}
\usage{
laplace_loglik(
  ad_pack,
  par,
  gamma,
  config = list(),
  control_inner = list(),
  deriv = TRUE
)
}
\arguments{
\item{ad_pack}{An \code{ad_pack} object.}

\item{par}{Numeric outer parameter vector \code{c(beta, theta)} on the
internal (log-theta) scale.}

\item{gamma}{Numeric vector of random-effect modes used as the inner start.}

\item{config}{Configuration list (may be empty).}

\item{control_inner}{Control list for the inner trust-region solve.}

\item{deriv}{Logical; if \code{TRUE} (default), return the profile gradient.}
}
\value{
A list with components
\describe{
  \item{log_lik}{Scalar Laplace-approximate marginal log-likelihood.}
  \item{gradient}{Numeric vector \eqn{\nabla_\psi \ell}, or \code{NULL} when
    \code{deriv = FALSE}.}
  \item{gamma}{Inner modes at the optimum.}
}
}
\description{
Thin wrapper around \code{\link{log_lik_laplace}} that returns the
\emph{positive} profile log-likelihood and its gradient with respect to the
outer parameters \code{c(beta, theta)}. Prefer this over reading
\code{inner_opt(...)$deriv$d_neg_log_lik} when building custom outer
objectives (e.g. modular / multi-pack likelihoods).
}
\seealso{
\code{\link{log_lik_laplace}}, \code{\link{inner_opt}},
  \code{\link{outer_fn}}
}
