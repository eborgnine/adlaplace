% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ad_model.R, R/model_setup.R
\name{ad_model}
\alias{ad_model}
\alias{model_setup}
\title{Set up model for adlaplace}
\usage{
ad_model(
  y = numeric(0),
  ATp = NULL,
  XTp = NULL,
  beta_map = empty_ngC(),
  gamma_map = empty_ngC(),
  theta_map = empty_ngC(),
  elgmMatrix = NULL,
  data = NULL,
  terms = list(),
  info = list()
)

model_setup(formula, data, verbose = FALSE)
}
\arguments{
\item{y}{Response vector (default empty).}

\item{ATp}{Transpose of random-effects design matrix (default empty).}

\item{XTp}{Transpose of fixed-effects design matrix (default empty).}

\item{beta_map}{Beta parameter map (\code{ngCMatrix}).}

\item{gamma_map}{Gamma parameter map (\code{ngCMatrix}).}

\item{theta_map}{Theta parameter map (\code{ngCMatrix}).}

\item{elgmMatrix}{Optional ELGM map (\code{ngCMatrix}).}

\item{data}{Data frame containing the variables}

\item{terms}{Model terms (default empty list).}

\item{info}{Parameter info (default empty list).}

\item{formula}{Model formula containing model terms}

\item{verbose}{Logical indicating whether to print verbose output (default: FALSE)}
}
\value{
An object of class \code{ad_model} (S4) from \code{\link{ad_model}}.
}
\description{
Function to set up model for adlaplace by extracting outcome variable and covariates,
creating design matrices, and preparing data structures for adlaplace optimization.
}
\section{Functions}{
\itemize{
\item \code{ad_model()}: Construct an ad_model object

}}
\examples{
\dontrun{
# Example usage
model_stuff <- model_setup(
  data = dat,
  formula = y ~ x1 + f(x2, model = "iwp", p = 2, knots = seq(0, 1, len = 11)) + f(fac, model = "iid")
)
}

}
