% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_ad_fun_raw.R
\name{get_ad_fun_raw}
\alias{get_ad_fun_raw}
\title{Build raw AD handle for one density shard}
\usage{
get_ad_fun_raw(data, config, kind, name, package = "adlaplace")
}
\arguments{
\item{data}{Model data list for this shard. For \code{kind = "obs"} (and
obs-style singles like \code{neg_binom_extra}), use observation data
(\code{y}, \code{ATp}, \code{XTp}, \code{theta_map}). For \code{kind = "single"}
with \code{name = "random_diagonal"}, use random data (\code{Q},
\code{theta_map}, \code{gamma_map}).}

\item{config}{Model configuration list passed to the backend builder.}

\item{kind}{Character scalar: \code{"obs"} or \code{"single"}.}

\item{name}{Registered density name (required; e.g. \code{"neg_binom_obs"},
\code{"neg_binom_extra"}, \code{"random_diagonal"}).}

\item{package}{Backend package that registered the density; currently only
\code{"adlaplace"} is supported.}
}
\value{
External pointer of class \code{adlaplace_handle_ptr}.
}
\description{
Constructs CppAD tapes for a single shard (observation groups or one
single-density term). Merge several partial handles with \code{\link{combine}}.
For the full model with \code{hessian_map}, use \code{\link{get_ad_fun}}.
}
\seealso{
\code{\link{combine}}, \code{\link{get_ad_fun}}
}
