% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/amip.R
\name{ad_pack_drop}
\alias{ad_pack_drop}
\title{Retape an AD pack after dropping observation units}
\usage{
ad_pack_drop(fit, drop, num_threads = NULL, verbose = FALSE)
}
\arguments{
\item{fit}{An \code{"adlaplace_fit"} from \code{\link{adlaplace}}.}

\item{drop}{Integer vector of **1-based** unit indices to remove.}

\item{num_threads}{OpenMP thread count; default from \code{fit$config}.}

\item{verbose}{Logical; forwarded into the retape config.}
}
\value{
An \code{ad_pack} usable with \code{\link{inner_opt}} at the
  original outer MLE.
}
\description{
Builds a new \code{\link{ad_pack}} on \code{fit$model_data} with
observation shards filtered to the remaining units (same coarse sharding
as \code{fit$config$obs_groups} when present). Random and parameter shards
are retaped; reuse of the merged fit pack is not attempted.
}
\seealso{
\code{\link{obs_groups_units}}, \code{\link{influence_scores}},
  \code{\link{amip_set}}
}
