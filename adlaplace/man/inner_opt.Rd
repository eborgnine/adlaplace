% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{inner_opt}
\alias{inner_opt}
\title{Inner optimization over gamma using trust-region CG (sparse)}
\usage{
inner_opt(
  parameters,
  gamma,
  ad_fun,
  control = NULL,
  deriv = FALSE,
  verbose = FALSE
)
}
\arguments{
\item{parameters}{Numeric vector of fixed outer parameters
(\code{beta}, \code{theta}; length \code{Nbeta+Ntheta}).}

\item{gamma}{Numeric vector of starting values for inner parameters
(\code{gamma}; length \code{Ngamma}).}

\item{ad_fun}{\code{ad_fun} S4 object from \code{\link{ad_fun}}.}

\item{control}{List of trust-region control parameters (see \pkg{trustOptim}).}

\item{deriv}{Logical; if \code{TRUE}, also return outer gradient/Hessian
pieces and Cholesky-based quantities at the inner mode.}

\item{verbose}{Logical; if \code{TRUE}, print thread/shard diagnostics.}
}
\value{
A list with \code{log_lik}, \code{neg_log_lik}, \code{parameters},
  \code{full_parameters}, \code{inner_opt}, \code{gradient}
  (list \code{inner}/\code{outer}), and \code{hessian}
  (list \code{inner}/\code{outer}/\code{chol_inner}/\code{half_log_det};
  with \code{deriv=TRUE} also \code{half_H_inv}, \code{H_inv}, \code{trace3}).
  Objective and derivatives use the negative log-density convention.
}
\description{
Runs the inner optimization over \eqn{\gamma} using the
\pkg{trustOptim} sparse trust-region CG solver on a pre-built
\code{\link{ad_fun}} handle.
}
\details{
Negates tape log-density values in C++ for minimization via
\pkg{trustOptim}.
}
