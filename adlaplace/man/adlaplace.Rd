% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{adlaplace}
\alias{adlaplace}
\title{Fit a hierarchical model by Laplace-approximate maximum likelihood}
\usage{
adlaplace(
  formula,
  data,
  config = list(),
  num_threads = 1L,
  num_shards = 100L,
  control = list(),
  control_inner = list(maxit = 100L, report.level = 0, report.freq = 0),
  method = "L-BFGS-B",
  hessian = TRUE,
  verbose = FALSE,
  na_omit = TRUE
)
}
\arguments{
\item{formula}{Model formula built from model terms, e.g.
\code{nbinom(y) ~ x + iid(g)}, a list of term objects from
\code{\link{collect_terms}}, or a precomputed \code{\link{model_data}}
result (in which case \code{data} is ignored).}

\item{data}{Data frame containing the variables referenced in \code{formula}.
Not needed when \code{formula} is already a \code{model_data} result.}

\item{config}{List of backend options merged over the defaults
\code{list(transform_theta = TRUE, verbose = verbose)}. When
\code{config$shards} is missing it is filled by
\code{ad_shards(A, num_shards = num_shards)}.}

\item{num_threads}{Positive integer, OpenMP thread count for the AD handle.}

\item{num_shards}{Target number of observation shards for
\code{\link{ad_shards}()}.}

\item{control}{List of \code{\link[stats]{optim}} control options, merged
over the defaults \code{list(maxit = 200L, parscale = <from terms>)}.}

\item{control_inner}{List of control options for the inner (random effects)
trust-region optimizer.}

\item{method}{\code{\link[stats]{optim}} method; bounds are used for
\code{"L-BFGS-B"} and \code{"Brent"} only.}

\item{hessian}{Logical. When \code{TRUE} (default), \code{optim} numerically
differentiates the exact AD profile gradient at the optimum, giving the
observed information used by \code{\link{vcov.adlaplace_fit}},
\code{\link{summary.adlaplace_fit}}, and
\code{\link{confint.adlaplace_fit}}.}

\item{verbose}{Logical, print progress.}

\item{na_omit}{Passed to \code{\link{model_data}()}.}
}
\value{
An object of class \code{"adlaplace_fit"}: a list with components
\describe{
  \item{call, formula}{The matched call and model formula.}
  \item{par}{Named outer parameter estimates on the optimization scale
    (log scale where \code{par_info$transform} is \code{TRUE}).}
  \item{par_info}{Parameter metadata table (\code{info$parameters}).}
  \item{vcov}{Inverse of the numerically differentiated Hessian of the
    negative log likelihood (optimization scale), or \code{NULL}.}
  \item{gamma}{Named random-effect modes at the optimum.}
  \item{logLik, nobs}{Laplace-approximate marginal log likelihood and the
    number of observations.}
  \item{optim}{Full \code{\link[stats]{optim}} result, including
    \code{hessian} when requested.}
  \item{details}{Result of \code{\link{log_lik_laplace}(deriv = TRUE)} at
    the optimum (gradient, Cholesky factors, inner solution).}
  \item{model_data, ad_fun, config, control_inner, cache}{Objects needed by
    the accessor methods and for warm restarts.}
}
}
\description{
One-call interface to the \pkg{adlaplace} pipeline: parse the formula with
\code{\link{model_data}()}, partition observations into AD shards with
\code{\link{ad_shards}()}, build the AD handle with \code{\link{ad_fun}()},
maximize the Laplace-approximate marginal likelihood with
\code{\link[stats]{optim}()} using the exact profile gradient
(\code{\link{outer_fn}} / \code{\link{outer_gr}}), and return a fitted-model
object with standard accessor methods.
}
\examples{
\dontrun{
fit <- adlaplace(
  nbinom(y, lower = 1e-9, init = 0.15) ~ x + iid(g, init = 0.1),
  data = dat, num_threads = 2L
)
summary(fit)
confint(fit)
}
}
\seealso{
\code{\link{summary.adlaplace_fit}}, \code{\link{predict.adlaplace_fit}},
  \code{\link{log_lik_laplace}}, \code{\link{model_data}}
}
