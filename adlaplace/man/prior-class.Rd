% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prior.R
\name{prior-class}
\alias{prior-class}
\alias{prior}
\title{Hyperparameter prior term}
\usage{
prior(theta = 0L, dist = "exp", median = 1, rate = NULL)
}
\arguments{
\item{theta}{Zero-based index into \code{info$theta$id}, or a character
theta label (e.g. \code{"ID_iid"}).}

\item{dist}{Prior family. Currently only \code{"exp"} (exponential on the
SD scale) is supported.}

\item{median}{Prior median on the SD scale. For \code{dist = "exp"}, the
rate is \code{log(2) / median}.}

\item{rate}{Optional exponential rate (overrides \code{median} when
supplied).}
}
\value{
A \code{prior} term object.
}
\description{
Places a prior on an existing theta (SD) parameter. Does not
create a new theta; it emits an \code{ad_kind = "parameters"} shard that
contributes to the joint log density.
}
