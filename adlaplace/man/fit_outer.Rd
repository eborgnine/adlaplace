% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{fit_outer}
\alias{fit_outer}
\title{Outer optimization scaffolding for Laplace profile likelihoods}
\usage{
fit_outer(
  fn,
  gr,
  par_meta,
  method = "L-BFGS-B",
  control = list(),
  hessian = FALSE,
  start = NULL,
  ...
)
}
\arguments{
\item{fn, gr}{Objective and gradient functions with signature
\code{function(par, ...)}.}

\item{par_meta}{Data frame of outer-parameter metadata with columns
\code{label}, \code{init}, \code{lower}, \code{upper}, \code{parscale},
\code{log} (as produced by \code{model_data()$term_data$info$parameters}).}

\item{method}{\code{\link[stats]{optim}} method.}

\item{control}{Control list for \code{optim}.}

\item{hessian}{Logical; compute a numerical outer Hessian via
\code{\link[stats]{optimHess}} when \code{TRUE}.}

\item{start}{Optional starting values on the internal scale; defaults to
\code{par_meta$init}.}

\item{...}{Extra arguments forwarded to \code{fn} / \code{gr} / \code{optimHess}
(e.g. \code{config}, \code{ad_pack}, \code{cache}, \code{control_inner}).}
}
\value{
A list with components \code{opt}, \code{par_info}, \code{vcov}, and
  \code{error} (character message when \code{optim} fails, otherwise
  \code{NULL}).
}
\description{
Runs \code{\link[stats]{optim}} on a user-supplied outer objective / gradient
(typically \code{\link{outer_fn}} / \code{\link{outer_gr}}, or a modular
average of several packs) and returns the standard \code{par_info} /
\code{vcov} / \code{opt} structure used by \code{\link{adlaplace}}.
}
\seealso{
\code{\link{adlaplace}}, \code{\link{outer_fn}}
}
