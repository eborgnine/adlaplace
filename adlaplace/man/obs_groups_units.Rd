% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/obs_groups.R
\name{obs_groups_units}
\alias{obs_groups_units}
\title{Build \code{obs_groups} for a subset of observation units}
\usage{
obs_groups_units(
  n_domain,
  units = NULL,
  grouping = c("identity", "together", "filter"),
  obs_groups = NULL
)
}
\arguments{
\item{n_domain}{Number of units in the full domain.}

\item{units}{Integer vector of 0-based unit indices to keep. Default all.}

\item{grouping}{One of:
\describe{
  \item{\code{"identity"}}{One shard column per selected unit (scores).}
  \item{\code{"together"}}{One shard containing all selected units.}
  \item{\code{"filter"}}{Start from \code{obs_groups}, drop unselected
    rows, drop empty columns (keeps coarse fit sharding).}
}}

\item{obs_groups}{Existing shard map; required when
\code{grouping = "filter"}.}
}
\value{
A \code{dgCMatrix} suitable for \code{config$obs_groups}.
}
\description{
Restricts which observation units are recorded on observation tapes.
Domain indices are **0-based** (ELGM stratum columns, or rows of \code{y}).
}
\seealso{
\code{\link{n_obs_units}}, \code{\link{grad_obs_units}},
  \code{\link{ad_pack_drop}}
}
