% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cond_simu.R
\name{cond_sim_iwp_at}
\alias{cond_sim_iwp_at}
\title{Conditional simulation for IWP/HIWP terms (flat arguments)}
\usage{
cond_sim_iwp_at(
  terms,
  random_info,
  beta,
  gamma_mode,
  half_H_inv,
  newx = NULL,
  n = 500,
  weights = NULL,
  probs = c(0.025, 0.5, 0.975),
  probs_envelope = c(0.1, 0.9)
)
}
\arguments{
\item{terms}{Named list of model term objects from \code{model_data()$terms}.}

\item{random_info}{Data frame from \code{model_data()$data$info$gamma} with
columns \code{gamma_label}, \code{term}, \code{model}, and \code{by}.}

\item{beta}{Named numeric vector of fixed-effect MLEs, or a data frame with
columns \code{beta_label} and \code{mle}.}

\item{gamma_mode}{Named numeric vector of random-effect modes at the Laplace
inner optimum, or a data frame with columns \code{gamma_label} and \code{mode}.}

\item{half_H_inv}{Matrix \eqn{H^{-1/2}} for inner random effects; from
\code{\link[adlaplace]{laplace_half_H_inv}(laplace)}.}

\item{newx}{Optional list of prediction data frames, one per variable.
When \code{NULL}, a default grid is built from term knot ranges.}

\item{n}{Number of conditional draws.}

\item{weights}{Optional group weights (named vector or list by variable).}

\item{probs}{Quantile levels for summaries.}

\item{probs_envelope}{Envelope probability levels (requires \pkg{GET}).}
}
\value{
List with simulated curves, quantiles, and envelopes.
}
\description{
Draw conditional simulations of IWP/HIWP/RSIWP model components from explicit
inputs. Prefer \code{\link{cond_sim_iwp}} when you have a Laplace \code{fit}
and \code{model_data} objects; use this function for custom pipelines.
}
