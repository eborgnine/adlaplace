% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dsun.R
\name{sun_mle}
\alias{sun_mle}
\title{SUN(3,3) MLE via C++ trust-region optimization}
\usage{
sun_mle(tape, start, control = list(), n_threads = 0L)
}
\arguments{
\item{tape}{An object from \code{\link[=dsun_fun]{dsun_fun()}}.}

\item{start}{Numeric start vector of length 21 (natural parameterization).}

\item{control}{List of trust-region controls (see \pkg{trustOptim}), e.g.
\code{maxit}, \code{grad.tol}, \code{report.freq},
\code{quasi.newton.method} (1 = SR1, 2 = BFGS).}

\item{n_threads}{Threads for shard evaluation; \code{0} uses the tape default.}
}
\value{
A list with \code{par}, \code{value} (log-likelihood),
\code{gradient}, \code{iterations}, \code{status}, and related fields.
}
\description{
Maximizes the taped SUN log-likelihood using the quasi-Newton trust-region
solver from \pkg{trustOptim} (SR1 by default). The \code{nu} components are
optimized on the log scale so that they remain positive. The entire
optimization runs in C++ with one OpenMP-parallel value+gradient evaluation
per iteration.
}
