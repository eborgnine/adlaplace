% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sun_kl.R
\name{fit_sun44_quad}
\alias{fit_sun44_quad}
\title{Fit SUN(4,4) by minimising KL on a weighted quadrature}
\usage{
fit_sun44_quad(
  quad,
  start,
  lower = NULL,
  upper = NULL,
  obj_scale = 10000,
  mc.cores = 1L,
  verbose = FALSE,
  mvquad_opts = list(),
  optim_opts = list()
)
}
\arguments{
\item{quad}{List with \code{x} (\eqn{n\times 3}), \code{w}, and optionally
\code{logp} (for returned KL).}

\item{start}{Length-36 start (see \code{\link[=make_sun44_start_from_normal]{make_sun44_start_from_normal()}}).}

\item{lower, upper}{Box constraints (defaults from \code{\link[=sun33_bounds]{sun33_bounds()}}).}

\item{obj_scale}{Scale applied to the optim objective (default \code{1e4}).}

\item{mc.cores}{OpenMP threads for the density tape.}

\item{verbose}{If \code{TRUE}, set \code{optim} \code{trace = 1}.}

\item{mvquad_opts}{Named list; used keys \code{n_points}, \code{n_shifts}
for orthant QMC (defaults \code{64}, \code{2}).}

\item{optim_opts}{Named list merged into \code{stats::optim} L-BFGS-B
control.}
}
\description{
Like \code{\link[=fit_sun33_quad]{fit_sun33_quad()}} for SUN(4,4) via \code{\link[=dsun44_fun]{dsun44_fun()}}. Default QMC
budget is \code{n_points = 256}, \code{n_shifts = 4}.
}
