% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dsun22.R
\name{dsun22}
\alias{dsun22}
\title{SUN(2,2) density with derivatives}
\usage{
dsun22(
  x,
  par,
  log = TRUE,
  deriv = 0L,
  n_points = 1021L,
  n_shifts = 8L,
  seed = 1L,
  n_threads = 1L,
  weights = NULL
)
}
\arguments{
\item{x}{Numeric vector of length 2 or an \eqn{n \times 2} matrix of
observations.}

\item{par}{Numeric vector of length 10; see \code{\link[=make_sun22_params]{make_sun22_params()}}.}

\item{log}{If \code{TRUE} (default), return the log-density / log-likelihood.}

\item{deriv}{Integer derivative order: \code{0} (default) value only,
\code{1} also gradient, \code{2} also Hessian, all with respect to
\code{par}.}

\item{n_points, n_shifts, seed}{Quasi-Monte Carlo settings for orthant CDFs.}

\item{n_threads}{Number of OpenMP threads for parallel shard evaluation.}

\item{weights}{Optional numeric vector of length \code{nrow(x)}.}
}
\value{
A list with \code{value} and \code{error}. If \code{deriv >= 1},
also \code{gradient} (length 10). If \code{deriv >= 2}, also
\code{hessian} (\eqn{10 \times 10}).
}
\description{
Evaluates the log-density (or density) of a SUN(2,2) distribution using the
block-Cholesky parameterization from \code{make_sun22_params()}. For a matrix
of observations the returned value is the summed log-likelihood, or a
weighted sum if \code{weights} is supplied. Derivatives are with respect to
the 10-component parameter vector only.
}
\seealso{
\code{\link[=make_sun22_params]{make_sun22_params()}}, \code{\link[=dsun22_fun]{dsun22_fun()}}, \code{\link[=dsun]{dsun()}}
}
