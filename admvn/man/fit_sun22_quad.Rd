% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sun_kl.R
\name{fit_sun22_quad}
\alias{fit_sun22_quad}
\title{Fit SUN(2,2) by minimising KL on a weighted quadrature}
\usage{
fit_sun22_quad(
  quad,
  start,
  lower = NULL,
  upper = NULL,
  obj_scale = 10000,
  mc.cores = 1L,
  verbose = FALSE,
  mvquad_opts = list(),
  optim_opts = list()
)
}
\arguments{
\item{quad}{List with \code{x} (\eqn{n\times 2}), \code{w}, and optionally
\code{logp} (for returned KL).}

\item{start}{Length-10 start (see \code{\link[=make_sun22_start_from_normal]{make_sun22_start_from_normal()}}).}

\item{lower, upper}{Box constraints (defaults from \code{\link[=sun22_bounds]{sun22_bounds()}}).}

\item{obj_scale}{Scale applied to the optim objective (default \code{1e4}).}

\item{mc.cores}{OpenMP threads for the density tape.}

\item{verbose}{If \code{TRUE}, set \code{optim} \code{trace = 1}.}

\item{mvquad_opts}{Named list; used keys \code{n_points}, \code{n_shifts}
for orthant QMC (defaults \code{64}, \code{2}).}

\item{optim_opts}{Named list merged into \code{stats::optim} L-BFGS-B
control.}
}
\value{
A list with \code{fit}, \code{par}, \code{dp}, \code{start},
\code{gradient} (objective-scale at \code{par}), KL / cross-entropy
summaries, and \code{engine}.
}
\description{
Minimises the weighted cross-entropy \eqn{-\sum_i w_i \log q(x_i)} with
analytic gradients from \code{\link[=dsun22_fun]{dsun22_fun()}}.
}
