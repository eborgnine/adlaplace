% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dsun44.R
\name{dsun44_fun}
\alias{dsun44_fun}
\title{Create a reusable SUN(4,4) log-likelihood tape}
\usage{
dsun44_fun(
  x,
  par_seed,
  n_points = 1021L,
  n_shifts = 8L,
  seed = 1L,
  n_threads = NULL,
  weights = NULL
)
}
\arguments{
\item{x}{Numeric vector of length 4 or an \eqn{n \times 4} matrix of
observations.}

\item{par_seed}{Seed parameter vector used when building the tape.}

\item{n_points, n_shifts, seed}{Quasi-Monte Carlo settings for orthant CDFs.}

\item{n_threads}{Default OpenMP threads for \code{$eval()}. When
\code{NULL}, uses the maximum available OpenMP thread count.}

\item{weights}{Optional numeric vector of length \code{nrow(x)}.}
}
\value{
An object of class \code{"admvn_sun44_tape"} with
\code{eval()} and \code{optim_fns()} (cached \code{fn}/\code{gr}).
}
\description{
Builds per-observation CppAD shard tapes for the (weighted) SUN(4,4)
log-likelihood with data \code{x} fixed. Use for KL / MLE: build once, then
call \code{$eval(par)} or \code{$optim_fns()}.
}
