% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dsun44.R
\name{dsun44}
\alias{dsun44}
\title{SUN(4,4) density with derivatives}
\usage{
dsun44(
  x,
  par,
  log = TRUE,
  deriv = 0L,
  n_points = 1021L,
  n_shifts = 8L,
  seed = 1L,
  n_threads = 1L,
  weights = NULL
)
}
\arguments{
\item{x}{Numeric vector of length 4 or an \eqn{n \times 4} matrix of
observations.}

\item{par}{Numeric vector of length 36; see \code{\link[=make_sun44_params]{make_sun44_params()}}.}

\item{log}{If \code{TRUE} (default), return the log-density / log-likelihood.}

\item{deriv}{Integer derivative order: \code{0} (default) value only,
\code{1} also gradient, \code{2} also Hessian, all with respect to
\code{par}.}

\item{n_points, n_shifts, seed}{Quasi-Monte Carlo settings for orthant CDFs.}

\item{n_threads}{Number of OpenMP threads for parallel shard evaluation.}

\item{weights}{Optional numeric vector of length \code{nrow(x)}.}
}
\value{
A list with \code{value} and \code{error}. If \code{deriv >= 1},
also \code{gradient} (length 36). If \code{deriv >= 2}, also
\code{hessian} (\eqn{36 \times 36}).
}
\description{
Evaluates the log-density (or density) of a SUN(4,4) distribution using the
block-Cholesky parameterization from \code{make_sun44_params()}. For a matrix
of observations the returned value is the summed log-likelihood, or a
weighted sum if \code{weights} is supplied. Derivatives are with respect to
the 36-component parameter vector only.
}
\details{
Orthant probabilities use Genz QMC with CppAD (dimension 4), so prefer a
modest \code{n_points}/\code{n_shifts} budget for speed.
}
\seealso{
\code{\link[=make_sun44_params]{make_sun44_params()}}, \code{\link[=dsun44_fun]{dsun44_fun()}}, \code{\link[=dsun]{dsun()}}
}
