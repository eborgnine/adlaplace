% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grf_bspline.R
\name{grf_bspline}
\alias{grf_bspline}
\alias{grf_bspline,list-method}
\alias{grf_bspline,data.frame-method}
\title{Tensor-product B-spline FEM ingredients for a 2D Matérn GRF}
\usage{
grf_bspline(coords, knots, degree = 2L, ...)

\S4method{grf_bspline}{list}(coords, knots, degree = 2L, ...)

\S4method{grf_bspline}{data.frame}(coords, knots, degree = 2L, ...)
}
\arguments{
\item{coords}{Evaluation locations. For a \code{data.frame} (e.g. \code{sites_eval} from
\code{expand.grid}), rows are point coordinates with columns \code{x} and \code{y}. For a
plain \code{list(x, y)} of axis sequences, the full tensor grid is formed with
\code{expand.grid}. With \strong{terra}, \code{SpatRaster} / \code{SpatVector} are also accepted.}

\item{knots}{Knot-line positions on each axis: \code{list(x = seq(...), y = seq(...))}
of unique increasing breakpoints (domain endpoints included). These are
expanded to open B-spline knot vectors. With terra, a \code{SpatRaster} whose
grid defines knot lines is also accepted.}

\item{degree}{B-spline degree; must be \verb{>= 2}. Default \code{2} (α = 2). Use
\code{degree = 3} when assembling \code{G3} for α = 3.}

\item{...}{Passed to methods (unused for \code{list} / \code{data.frame}).}
}
\value{
A list with:
\describe{
\item{A}{\code{dgCMatrix} design, \code{n} by \code{n_x * n_y}}
\item{C, G, G2}{sparse Grams}
\item{G3}{sparse Gram or \code{NULL} if \code{degree < 3}}
\item{degree, knots, n_basis}{metadata (\code{n_basis = c(n_x, n_y)})}
}
}
\description{
Builds the design matrix of tensor-product B-splines at evaluation
coordinates together with sparse mass, stiffness, and higher-order Gram
matrices used in the SPDE FEM precision for operator orders α = 2 and 3.
}
\details{
Coefficient ordering is column-major (\code{vec}) over the \code{n_x} by \code{n_y} array of
tensor basis indices: basis \verb{(i, j)} maps to column \code{i + (j - 1) * n_x}.
}
\examples{
sites_list <- list(x = seq(0, 1, by = 0.25), y = seq(0, 1, by = 0.25))
sites_eval <- do.call(expand.grid, sites_list)
fem <- grf_bspline(sites_eval, sites_list, degree = 2L)

}
