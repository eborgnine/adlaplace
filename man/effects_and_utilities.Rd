% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/effects.R
\name{effects_and_utilities}
\alias{effects_and_utilities}
\alias{iwp}
\alias{iwpDesign}
\alias{iwpPrecision}
\alias{iwpTheta}
\alias{hiwp}
\alias{hiwpDesign}
\alias{hiwpPrecision}
\alias{hiwpTheta}
\alias{fpoly}
\alias{fpolyDesign}
\alias{rpoly}
\alias{rpolyDesign}
\alias{rpolyPrecision}
\alias{rpolyTheta}
\alias{hrpoly}
\alias{hrpolyDesign}
\alias{hrpolyPrecision}
\alias{hrpolyTheta}
\alias{bs}
\alias{od}
\alias{odDesign}
\alias{odPrecision}
\alias{odTheta}
\title{Functions to specify terms of the formula (and associated utility functions)}
\usage{
iwp(x, p = 2, ref_value, knots, range = NULL, rpoly_p = 0, fpoly_p = 1)

iwpDesign(term, data)

iwpPrecision(term)

iwpTheta(theta_info, term)

hiwp(
  x,
  p = 2,
  ref_value,
  knots,
  range = NULL,
  group_var,
  include_global = T,
  hrpoly_p = p - 1,
  hfpoly_p = 0,
  rpoly_p = 0,
  fpoly_p = include_global
)

hiwpDesign(term, data)

hiwpPrecision(term)

hiwpTheta(theta_info, term)

fpoly(x, p = 2, ref_value = 0)

fpolyDesign(term, data)

rpoly(x, p = 2, ref_value)

rpolyDesign(term, data)

rpolyPrecision(term)

rpolyTheta(theta_info, term)

hrpoly(x, p = 1, ref_value, group_var, include_global = T)

hrpolyDesign(term, data)

hrpolyPrecision(term)

hrpolyTheta(theta_info, term)

bs(x, o = 2, ref_value, range = NULL, nknots = NULL, run_as_is = T, ...)

od(x)

odDesign(term, data)

odPrecision(term)

odTheta(theta_info, term)
}
\arguments{
\item{x}{A numeric vector representing the data to which the model is applied.}

\item{p}{A numeric value specifying the polynomial degree (default: varies by function).}

\item{ref_value}{A numeric value specifying the reference point for the basis functions.}

\item{knots}{A numeric vector specifying the knot locations for spline-based models.}

\item{range}{A numeric vector of length 2 specifying the range of the data (optional).}

\item{rpoly_p}{A numeric value specifying the degree for (random effects) polynomial components to add to the model as a separate term.}

\item{fpoly_p}{A numeric value specifying the degree for (fixed effects) polynomial components to add to the model as a separate term.}

\item{term}{A list representing the term structure for the design matrix or precision calculations.}

\item{data}{A data frame containing the data to which the model terms are applied.}

\item{theta_info}{A list containing information about parameter indexing and initialization for hierarchical models.}

\item{group_var}{A factor or grouping variable used to define hierarchical structures (optional).}

\item{include_global}{A logical value indicating whether to include a global component in hierarchical models (default: \code{TRUE}).}

\item{hrpoly_p}{A numeric value specifying the degree for hierarchical (random effects) polynomial components to add to the model as a separate term.}

\item{hfpoly_p}{A numeric value specifying the degree for hierarchical (fixed effects) polynomial components to add to the model as a separate term.}

\item{...}{Additional arguments passed to other methods or functions.}
}
\value{
A list or matrix, depending on the function, representing the design matrix, precision matrix, or parameter information for the specified model term.
}
\description{
This suite of functions implements various moels (fixed and random effects) polynomials, integrated Wiener processes, hierarchical extensions.
}
\details{
These functions provide polynomial and spline-based models:
\itemize{
\item \strong{Raw Polynomial Functions (rpoly, hrpoly)}: Construct design matrices for raw polynomial terms (random effects polynomial terms), with hierarchical extensions available for group-specific modeling.
\item \strong{Fitted Polynomial Functions (fpoly, hfpoly)}: Generate fitted polynomial terms (fixed effects polynomial terms) that adapt to specific data features, also with hierarchical extensions.
\item \strong{Integrated Wiener Process (iwp, hiwp)}: Develop design and precision matrices for integrated Wiener process components, with options for hierarchical group structures.
\item \strong{Spline-based Models (splines, knots)}: Support for spline terms, allowing for flexible non-linear modeling with specified knot locations.
\item \strong{Utility Functions}:
\itemize{
\item \strong{Design Functions (\verb{*Design})}: Construct design matrices tailored to specific modeling terms.
\item \strong{Precision Matrix Functions (\verb{*Precision})}: Construct precision matrices corresponding to specific modeling terms.
\item \strong{Theta Functions (\verb{*Theta})}: Handle the variance parameters associated with random effects.
}
}

The utility functions are specifically used within the \code{hnlm} function and are not be exported.
}
\examples{
These were generated with chatGPT and have not been reviewed. See vignette for a human generated example.

# Example usage for iwp
term <- iwp(x = 1:10, ref_value = 5, knots = c(0, 5, 10))
design_matrix <- iwpDesign(term, data = data.frame(x = 1:10))

# Example usage for hierarchical model (hiwp)
term <- hiwp(x = 1:10, ref_value = 5, knots = c(0, 5, 10), group_var = factor(rep(1:2, each = 5)))
design_matrix <- hiwpDesign(term, data = data.frame(x = 1:10, group_var = factor(rep(1:2, each = 5))))

# Example usage for raw polynomial models
term <- rpoly(x = 1:10, ref_value = 5, p = 2)
design_matrix <- rpolyDesign(term, data = data.frame(x = 1:10))

# Example usage for hierarchical raw polynomial (hrpoly)
term <- hrpoly(x = 1:10, ref_value = 5, group_var = factor(rep(1:2, each = 5)))
design_matrix <- hrpolyDesign(term, data = data.frame(x = 1:10, group_var = factor(rep(1:2, each = 5))))

# Example usage for fitted polynomial models
term <- fpoly(x = 1:10, ref_value = 5, p = 3)
design_matrix <- fpolyDesign(term, data = data.frame(x = 1:10))

# Example usage for hierarchical fitted polynomial (hfpoly)
term <- hfpoly(x = 1:10, ref_value = 5, group_var = factor(rep(1:2, each = 5)))
design_matrix <- hfpolyDesign(term, data = data.frame(x = 1:10, group_var = factor(rep(1:2, each = 5))))
}
